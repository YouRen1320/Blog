# V1.1 完成报告 —— RAG + LangGraph Agent

**日期**:2026-05-05
**Tag**:v1.1.0
**前置版本**:v1.0.0(V2 真上线)

## 范围

在已经上线的 v1.0.0 之上,把"用户口语 V3"承诺的两件事补上:**RAG 检索 + LangGraph Agent**。

## 完成清单

### 数据层
- [x] Postgres 镜像换 `pgvector/pgvector:pg16`(向后兼容,数据卷不变)
- [x] `articles.embedding` 列(`vector(512)`),BGE-small-zh-v1.5 输出维度
- [x] migration 含 `CREATE EXTENSION IF NOT EXISTS vector` 保证全新部署
- [x] 现有「第一灯」文章已 backfill embedding

### ai-service
- [x] `fastembed` + BAAI/bge-small-zh-v1.5(本地 ONNX,不依赖外部 API)
- [x] `POST /embed` 端点,首次启动自动从 hf-mirror.com 下载模型
- [x] `services/retriever.py` —— 直读 Postgres 做 cosine 距离检索
- [x] `services/agent.py` —— LangGraph StateGraph,`retrieve → generate` 节点链
- [x] `/generate/article` 入口换成 `run_agent`,失败有 graceful fallback

### NestJS api
- [x] `EmbeddingModule`(global)封装 ai-service `/embed` 调用 + raw SQL UPDATE 写向量
- [x] `ArticlesService.publish()` 后 fire-and-forget 算 embedding(非阻塞)
- [x] articles.service.spec.ts 加 `EmbeddingService` mock,13/13 unit pass

### admin
- [x] **修了一个生产 bug**:SPA build 时没注入 VITE_API_BASE_URL,默认指向 localhost:3000 → 浏览器在 admin.iyouren.top 加载时连不通 API
- [x] 修法:运行时根据 `window.location.hostname` 兜底,生产/开发同一份镜像

### 部署
- [x] postgres 镜像热替换(volume 复用,数据零损失)
- [x] api + ai-service 重 build + 重启,6 容器全部 Up healthy
- [x] mock 模式整条 LangGraph 链路在生产端验证

## 端到端验证(mock 模式)

```
$ admin /inbox 触发"AI 帮我生成"
→ POST /api/admin/ai/drafts
→ NestJS AiService → ai-service /generate/article
→ LangGraph: START → retrieve_node → generate_node → END
   - retrieve_node: BGE 模型加载完成,/embed 跑通
   - generate_node: mock 模式直接返回 [MOCK] 草稿
→ NestJS 落库 status=DRAFT, source=AI
→ admin AIInbox 看到新草稿
```

mock 模式整条链路通,**RAG 索引已就绪**,真 LLM 端到端验证(见下)。

## v1.1.1 补丁:真 LLM 链路打通(MiMo 是 OpenAI 协议)

**根因**:之前一直按"小米 claudecode 集成"字面意思走 Anthropic SDK + `/messages`,各种 endpoint 都 401。用户给出官方代码示例后才发现 **MiMo 是 OpenAI 协议**:

- base_url: `https://api.xiaomimimo.com/v1`(SDK 自动追加 `/chat/completions`)
- model: `mimo-v2.5-pro`(推理模型,返回 `reasoning_content`)
- 鉴权: `Authorization: Bearer <api_key>`(OpenAI SDK 默认)

**改动**(commit `67459cf` + `7722334` + `14700fd`):
- 依赖:`anthropic` → `openai>=1.50.0`
- `mimo_client.py`:`AsyncAnthropic` → `AsyncOpenAI`
- `article_generator.py` + `agent.py`:`messages.create` → `chat.completions.create`,
  tool 定义从 `{name, input_schema}` 改成 `{type:function, function:{name, parameters}}`,
  tool_call.arguments 是 JSON 字符串需 `json.loads`
- Dockerfile:去掉 `RUN ... | tail -3` 形式的预下载(吞错误码),BGE 改运行时下载
- `retriever.py`:剥掉 Prisma 风格的 `?schema=public` query 参数(psycopg 不识别)

**生产验证**(2026-05-05):
```
prompt: "想聊聊我在做这个个人博客时,第一篇文章想表达的初心和坚持"
[retrieve] 1 articles above threshold(命中第一灯)
POST https://api.xiaomimimo.com/v1/chat/completions 200 OK(~25s)
[generate] draft generated: 写给自己的第一篇
```

LangGraph 节点全跑,RAG 上下文成功注入,生成的草稿语气抒情、与第一灯主题契合。

## 关键决策

### 1. 不引入 LangChain 主仓库,只装 LangGraph + langchain-core
LangChain 主仓库 1000+ 个集成包,过度抽象、API churn。LangGraph 是它的 stateful agent 子项目,可以单独用 + 类型清晰。

### 2. 本地 BGE 不用第三方 embedding API
- 中国境内访问 OpenAI / Voyage 不稳
- 小米 MiMo 是 Claude 协议(只有 messages 没 embeddings)
- 阿里云 dashscope 要单独 key + 收费
- BGE-small-zh-v1.5 是开源模型,fastembed 用 ONNX 跑 100MB / CPU 推理 < 100ms
- **不烧 quota,中文好,首次部署多 100MB 镜像**

### 3. 直接 psycopg + raw SQL,不用 SQLAlchemy ORM
- ai-service 只 SELECT + 比较向量,逻辑非常简单
- 引 ORM 多一层抽象、多一份 schema 来源
- **psycopg + pgvector 库 + cosine `<=>` 操作符,30 行代码搞定**

### 4. embedding 在 publish 后异步触发
`void this.embedding.embedArticle(id)`(没 await),用户的 publish 接口不被 RAG 索引拖慢。失败时只 warn,后续可以批量补。

### 5. 同一份 admin SPA 镜像走生产/开发
- baseURL 运行时探测 `window.location.hostname`
- 不需要给生产 build 单独传 build arg
- 镜像跨环境复用,CI 简单

## 踩坑记录

1. **BGE-small-zh-v1.5 输出 512 维,不是 384**(常以为 small=384)。Schema/Migration 错过一次,改了
2. **macOS Python 3.14 太新**,fastembed 的 transitive `py-rust-stemmers` 没 wheel,需要 Python 3.12 venv
3. **fastembed HuggingFace 下载默认走 huggingface.co**,中国境内不稳,要 `HF_ENDPOINT=https://hf-mirror.com`
4. **Dockerfile 预下载模型**:`RUN python -c "..." | tail -3` 把错误吞了,导致 cache 没下到。后来去掉 pre-download,改成运行时下载
5. **服务器 git pull GitHub 还是不稳**,关键改动用 `tar | scp` 替代
6. **Debian trixie 用 DEB822 sources.list.d**,sources.list 写了不读
7. **生产 admin SPA baseURL 没 build arg 注入**:这是用户实际遇到的"登录报错"的原因,本次报告里有详细修法
8. **MiMo 协议错判**:看到"claudecode 集成"就走 Anthropic SDK,实际 MiMo 是 OpenAI 协议(`/v1/chat/completions`)。教训:不要从产品名 / 文档名推断协议,直接 curl 试一下 endpoint 就知道
9. **`DATABASE_URL` 跨语言不通用**:Prisma 自加 `?schema=public`,psycopg 不认这个 URI 参数,retrieve 节点直接 fail。修法:psycopg 这边剥掉 query string

## 测试矩阵

| 层 | 通过 |
|---|---|
| api unit (jest) | 13/13 |
| ai-service pytest | 5/5 |
| 生产 mock 链路 | ✅ |
| **生产真 LLM(v1.1.1)** | **✅ MiMo + RAG 命中 + LangGraph 端到端通** |

## V0 → V1.1 全套 tag

| tag | 阶段 |
|-----|------|
| v0.1.0 | V1 本地闭环 |
| v0.3.0 | V3 Flutter 移动端 |
| v0.4.0 | V4 AI 内容生产(基础 LLM 调用) |
| v0.5.0 | Final 生产化加固 |
| v1.0.0 | V2 真上线 |
| v1.1.0 | AI2:RAG + LangGraph Agent(mock 链路) |
| **v1.1.1** | **AI2 补丁:OpenAI SDK + 真 LLM 端到端通** |

下一站候选(等你拍板):
- 后台 settings 加"改密码"页(目前点会报错)
- 后台 admin 加"批量回填 embedding"按钮(给老文章补向量)
- 移动端实测 admin 生成 + 提交链路(目前应该已经能用,但没真机跑过)
- LangSmith tracing 接入(看每次生成 token 花在哪儿)
